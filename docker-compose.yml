services:

  backend:
    build:
      context: ./backend
    container_name: portfolio-backend
    restart: unless-stopped
    ports:
      - "8000:8000"

  frontend:
    build:
      context: .
    container_name: portfolio-frontend
    restart: unless-stopped
    ports:
      - "5173:80"
    depends_on:
      - backend